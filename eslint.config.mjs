// This is a stub ESLint configuration file
console.log(
  `[Info  - ${new Date().toLocaleTimeString('en-US', { hour12: true })}] Loading ESLint stub configuration file from the root.\n` +
  "   If ESLint is not installed globally, you may see a 'Failed to load the ESLint library' error for this file.\n" +
  "   This behavior is expected and normal. Please ensure ESLint is installed locally in each subproject (e.g., frontend/, aws-deploy/)."
);
export default [{}]
